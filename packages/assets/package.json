{
  "name": "assets",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build-assets": "npx @svgr/cli -- src/assets",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@svgr/cli": "^8.0.1",
    "svgo": "^3.0.2",
    "svgo-autocrop": "^1.1.2",
    "typescript": "^5.9.2"
  }
}