{
  "$schema": "https://turborepo.org/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "dist/**",
        ".next/**"
      ],
      "dependsOn": [
        "^build"
      ],
      "env": [
        "NODE_ENV",
        "SN_ENV",
        "STADNAMN_ES_ENDPOINT",
        "STADNAMN_ES_TOKEN",
        "ES_TOKEN_TEST",
        "ES_TOKEN",
        "ES_ENDPOINT",
        "ES_ENDPOINT_TEST",
        "STADNAMN_CACHE_TOKEN",
        "VERCEL_ENV"
      ]
    }
  }
}