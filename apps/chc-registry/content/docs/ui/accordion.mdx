---
title: Accordion
description: A Accordion component
---

## Preview

<Tabs items={['preview', 'code']}>
  <Tab>
    <ComponentPreview name='accordion-demo'/>
  </Tab>
  <Tab>
    <ComponentSource name='accordion-demo' />
  </Tab>
</Tabs>

## Installation

### CLI

<Tabs items={['npm', 'pnpm']}>
  <Tab>
    ```sh
npx shadcn@latest add https://chc-registry.vercel.app/r/accordion.json
    ```
  </Tab>
  <Tab>
    ```sh 
pnpm dlx shadcn@latest add https://chc-registry.vercel.app/r/accordion.json 
    ```
  </Tab>
</Tabs>

### Manual

<Steps>

<Step>Install the following dependencies</Step>

<Tabs items={['npm', 'pnpm']}>
  <Tab>
    ```sh
npm install @base-ui/react
    ```
  </Tab>
  <Tab>
    ```sh 
pnpm add @base-ui/react
    ```
  </Tab>
</Tabs>

<Step>Add a classname utility function</Step>

```tsx
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export const cn = (...inputs: ClassValue[]) => {
  return twMerge(clsx(inputs));
};
```

<Step>Copy and paste the following code into your project</Step>

<ComponentSource name='accordion' />

<Step>Update the import paths to match your project setup</Step>

</Steps>
