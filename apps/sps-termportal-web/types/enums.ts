export enum HeaderSize {
  Minimal = "minimal",
  Default = "default",
  Full = "full",
}

export enum FetchType {
  Autocomplete = "autocomplete",
  Inital = "initial",
  Options = "options",
  Filter = "filter",
  Further = "further",
}
