import React from 'react'

const Header = (props) => {
  return (
    <header>
      <h1>Import resources (because of a bug you need to reload the page after choosing another source!)</h1>
    </header>
  )
}

export default Header
